# Map peak sequence names to a genome

Map peak sequence names to a genome

## Usage

``` r
map_peak_ranges_to_genome(peak_ranges, genome)
```

## Arguments

- peak_ranges:

  Genomic ranges to map.

- genome:

  A genome object with sequence levels and lengths.

## Value

A list of mapped ranges, support flags and original/mapped sequence
names.
