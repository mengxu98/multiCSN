# Get stored edge_uniqueness results from CSNObject

Get stored edge_uniqueness results from CSNObject

## Usage

``` r
EdgeUniqueness(object, ...)

# S4 method for class 'Seurat'
EdgeUniqueness(object, network = DefaultNetwork(object), ...)
```

## Arguments

- object:

  The input data.

- ...:

  Arguments for other methods

- network:

  Name of the network.
