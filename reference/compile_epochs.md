# compile_epochs

compile networks based on matching network label in result of running
assign_network.

## Usage

``` r
compile_epochs(networks_list)
```

## Arguments

- networks_list:

  list of results of running assign_network on each path.

## Value

a compiled list of network genes
