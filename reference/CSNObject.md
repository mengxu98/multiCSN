# Show a compact summary of CSNObject state stored in a Seurat object

Show a compact summary of CSNObject state stored in a Seurat object

## Usage

``` r
CSNObject(object, ...)
```

## Arguments

- object:

  A `Seurat` object initialized by `multiCSN`.

- ...:

  Unused.

## Value

Invisibly returns `object`.
