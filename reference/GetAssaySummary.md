# Get summary of seurat assay

Get summary of seurat assay

Get summary of seurat assay

## Usage

``` r
GetAssaySummary(object, ...)

GetAssaySummary(object, ...)

# S4 method for class 'Seurat'
GetAssaySummary(object, group_name, assay = NULL, verbose = TRUE, ...)

# S4 method for class 'CSNObject'
GetAssaySummary(object, group_name, assay = NULL, verbose = TRUE, ...)
```

## Arguments

- object:

  The input data, a csn object.

- ...:

  Arguments for other methods

- group_name:

  group_name

- assay:

  assay

- verbose:

  verbose
