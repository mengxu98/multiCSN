# Get active network

Get active network

Get active network name

## Usage

``` r
DefaultNetwork(object, ...)

DefaultNetwork(object, ...)

DefaultNetwork(object, ...) <- value

# S4 method for class 'Seurat'
DefaultNetwork(object, ...)

# S4 method for class 'Seurat'
DefaultNetwork(object) <- value
```

## Arguments

- object:

  The input data, a csn object.

- ...:

  Arguments for other methods

- value:

  Name of the network to activate.
