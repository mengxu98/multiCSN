% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density_intersection.R
\name{density_points}
\alias{density_points}
\title{Partition pseudotime states by density intersections}
\usage{
density_points(
  object = NULL,
  pseudotime_column = "pseudotime",
  group_column = NULL,
  meta_data = NULL,
  store = TRUE,
  overwrite = FALSE,
  posterior_threshold = 0.8,
  n_grid = 512,
  n_boot = 50,
  bandwidth = "nrd0",
  plot = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{Seurat} object or a metadata \code{data.frame}.}

\item{pseudotime_column}{Column name containing pseudotime values.}

\item{group_column}{Optional metadata column containing ordered groups.
When \code{NULL}, the function reuses \code{celltype},
\code{seurat_clusters}, \code{cluster}, or \code{group} when available.}

\item{meta_data}{Deprecated alias of \code{object} for backwards compatibility.}

\item{store}{Logical. When \code{object} is a \code{Seurat} object, store the
partition result in \code{object@tools}.}

\item{overwrite}{Logical. Overwrite an existing stored partition.}

\item{posterior_threshold}{Maximum pairwise posterior dominance allowed inside
a transition window. Lower values make transition windows narrower.}

\item{n_grid}{Number of grid points used for kernel density evaluation.}

\item{n_boot}{Number of bootstrap resamples used to assess boundary stability.}

\item{bandwidth}{Bandwidth passed to \code{stats::density()}.}

\item{plot}{Logical. When \code{TRUE}, call \code{plot_states()} after
computing the partition.}

\item{verbose}{Logical.}
}
\value{
If \code{object} is a \code{Seurat} object, returns the updated
  object. Otherwise returns the partition result list.
}
\description{
Partition pseudotime states by density intersections
}
\examples{
test_data <- rbind(
  data.frame(cluster = "cluster1", pseudotime = rnorm(500, mean = 1, sd = 1)),
  data.frame(cluster = "cluster2", pseudotime = rnorm(500, mean = 2, sd = 1)),
  data.frame(cluster = "cluster3", pseudotime = rnorm(500, mean = 3, sd = 1))
)
density_points(test_data, pseudotime_column = "pseudotime", group_column = "cluster")
}
