% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perturbation_truth.R
\name{pseudobulk_perturbation}
\alias{pseudobulk_perturbation}
\title{Pseudobulk a perturbation screen into a paired design}
\usage{
pseudobulk_perturbation(
  cells,
  sample_info,
  targets = NULL,
  control_label = "NT",
  roles = c(control = "network_training_control", perturbed = "heldout_perturbation"),
  require_complete = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{cells}{A matrix (features x cells) or a named list of matrices, one per
modality (for example RNA and ATAC). All modalities must share the cells.}

\item{sample_info}{Data frame with \code{cell_id}, \code{replicate},
\code{timepoint} and \code{target} columns.}

\item{targets}{Perturbation targets to aggregate; defaults to every target of
\code{sample_info} except the control label, in order of appearance.}

\item{control_label}{Label identifying the unperturbed control cells.}

\item{roles}{Named character vector mapping \code{control} and
\code{perturbed} to the role labels stored in the sample metadata.}

\item{require_complete}{Fail when a replicate lacks a control or target
sample.}

\item{verbose}{Print progress messages.}
}
\value{
A list with \code{sample_metadata} and \code{pseudobulk} (one matrix
  per modality, columns ordered exactly like \code{sample_metadata$sample_id}).
}
\description{
Sums single-cell counts of one or more modalities per
replicate x condition sample, which is the input of the paired effect model
used to validate an inferred network against a perturbation screen. The
control label keeps its own sample in every replicate, so the resulting
design is paired and complete by construction.
}
