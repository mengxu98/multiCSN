% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_transitions.R
\name{classify_edge_transitions}
\alias{classify_edge_transitions}
\title{Classify edges between two network states}
\usage{
classify_edge_transitions(
  previous,
  next_state,
  keys,
  direction_column = "direction",
  labels = c(gained = "gained", lost = "lost", stable = "shared_sign_stable", flip =
    "shared_sign_flip")
)
}
\arguments{
\item{previous, next_state}{Endpoint tables with the key columns and a signed
direction column.}

\item{keys}{Key columns identifying an edge.}

\item{direction_column}{Column holding the signed direction in both tables.}

\item{labels}{Named character vector with the class names for
\code{gained}, \code{lost}, \code{stable} and \code{flip}.}
}
\value{
A \code{data.table} with the key columns,
  \code{previous_direction}, \code{next_direction} and \code{topology_class};
  edges present in only one state have \code{NA} on the other side.
}
\description{
Merges two endpoint tables on their key columns and labels every edge as
gained, lost, sign-stable or sign-flipped. This is the shared reduction behind
the state-transition, donor-deletion and cell-budget summaries.
}
