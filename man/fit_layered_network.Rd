% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_layered_network.R
\name{fit_layered_network}
\alias{fit_layered_network}
\title{Fit a layered multi-omic gene regulatory network}
\usage{
fit_layered_network(
  object,
  cell_group = NULL,
  cells = NULL,
  regulators = NULL,
  targets = NULL,
  min_detected = 0L,
  min_peak_cells = 3L,
  upstream = 1e+05,
  downstream = 0L,
  tf_region_scope = c("motif_peaks", "domain_peaks"),
  exclude_response_alias = FALSE,
  renormalize = FALSE,
  response_chunk = 1024L,
  max_support_size = NULL,
  min_improvement = 1e-10,
  sort_regulators = FALSE,
  cores = 1L,
  checkpoint_dir = NULL,
  store = FALSE,
  network_name = "multiCSN",
  verbose = TRUE
)
}
\arguments{
\item{object}{A Seurat object with chromatin accessibility and gene expression
assays plus the multiCSN state (see \code{\link{initiate_object}}).}

\item{cell_group}{Catalogue key (cell type, state or timepoint) used to select
cells, peaks and target genes.}

\item{cells}{Optional character vector of exact cells to fit; when supplied the
fit uses exactly these cells and validates them against \code{cell_group}.}

\item{regulators, targets}{Optional character vectors restricting the
transcription factors and target genes.}

\item{min_detected}{Minimum number of cells in which a target gene or a
transcription factor must be detected; \code{0} disables the filter.}

\item{min_peak_cells}{Minimum number of cells in which a candidate regulatory
region must be detected.}

\item{upstream, downstream}{Window in base pairs used to assign candidate
regions to genes.}

\item{tf_region_scope}{Candidate regions of the TF-region layer:
\code{"motif_peaks"} fits every motif-carrying accessible region, while
\code{"domain_peaks"} restricts the layer to regions inside gene domains.}

\item{exclude_response_alias}{Drop candidates whose standardized cross-product
with the response is an affine alias (|r| >= 1 - 1e-10).}

\item{renormalize}{Re-normalize RNA counts and re-run TF-IDF on the selected
cells before fitting.}

\item{response_chunk}{Number of targets fitted per chunk.}

\item{max_support_size, max_improvement}{Greedy-l0 stopping rules forwarded to
\code{inferCSN::fit_greedy_l0_batch()}.}

\item{sort_regulators}{Sort the transcription factors before fitting.}

\item{cores}{Number of forked workers used per chunk (ignored on Windows).}

\item{checkpoint_dir}{Optional directory storing per-chunk checkpoints so an
interrupted fit can resume; \code{NULL} keeps everything in memory.}

\item{store}{Store the three layers as \code{Network} entries in the returned
object.}

\item{network_name}{Name of the stored network entries.}

\item{verbose}{Print progress messages.}
}
\value{
A list with \code{tf_gene}, \code{tf_region}, \code{region_gene},
  \code{triplets}, \code{mediated_tf_gene}, \code{accounting}, \code{summary},
  \code{settings}, \code{cells} and, when \code{store = TRUE}, \code{object}.
}
\description{
Fits the layered multiCSN model on single-cell multiome data: a
transcription-factor-to-gene layer, a transcription-factor-to-region layer and
a region-to-gene layer, each solved as independent per-target Greedy-l0
problems, followed by the factorized chain projection that links the three
layers. By default only the selected cells are used and the expression layers
stored in the object are taken as-is.
}
