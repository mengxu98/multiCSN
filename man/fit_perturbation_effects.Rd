% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perturbation_truth.R
\name{fit_perturbation_effects}
\alias{fit_perturbation_effects}
\title{Fit paired perturbation effects on pseudobulk counts}
\usage{
fit_perturbation_effects(
  pseudobulk,
  sample_metadata,
  targets = NULL,
  timepoints = NULL,
  control_label = "NT",
  effect_definition = "heldout_perturbed_minus_NT_control",
  min_replicates = 3L,
  output_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{pseudobulk}{Named list of pseudobulk matrices (see
\code{\link{pseudobulk_perturbation}}).}

\item{sample_metadata}{Sample metadata with \code{sample_id}, \code{replicate},
\code{timepoint}, \code{target}, \code{cells}.}

\item{targets, timepoints}{Subsets to fit; defaults to every target and
timepoint present.}

\item{control_label}{Label of the control samples.}

\item{effect_definition}{Label stored in the effect tables.}

\item{min_replicates}{Minimum number of paired replicates per contrast.}

\item{output_dir}{Optional directory receiving one \code{<timepoint>__<target>.tsv.gz}
table per contrast; when \code{NULL} the tables are returned in memory.}

\item{verbose}{Print progress messages.}
}
\value{
A list with \code{effects} (per modality, one table or path per
  contrast) and \code{summary} (one row per modality x timepoint x target).
}
\description{
Fits one quasi-likelihood negative-binomial model per timepoint and target
with a paired design (\code{~ replicate + condition}) and reports per-feature
log fold changes and FDR values for the perturbed-minus-control contrast.
}
