% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_visualization.R
\name{plot_dynamic_networks}
\alias{plot_dynamic_networks}
\alias{plot_dynamic_networks,Seurat-method}
\alias{plot_dynamic_networks,CSNObject-method}
\alias{plot_dynamic_networks,data.frame-method}
\title{Plot dynamic networks}
\usage{
plot_dynamic_networks(object, ...)

\S4method{plot_dynamic_networks}{Seurat}(
  object,
  network = NULL,
  celltypes = NULL,
  weight_cutoff = NULL,
  celltypes_order = NULL,
  ntop = 10,
  filter_by_ntop = TRUE,
  ...
)

\S4method{plot_dynamic_networks}{CSNObject}(object, ...)

\S4method{plot_dynamic_networks}{data.frame}(
  object,
  celltypes_order = NULL,
  ntop = 10,
  filter_by_ntop = TRUE,
  title = NULL,
  theme_type = "theme_void",
  plot_type = "ggplot",
  layout = "fruchtermanreingold",
  nrow = 2,
  ncol = NULL,
  byrow = TRUE,
  combine = TRUE,
  figure_save = FALSE,
  figure_name = NULL,
  figure_width = 6,
  figure_height = 6,
  seed = 1
)
}
\arguments{
\item{object}{Data.frame with columns regulator, target, weight, celltype.}

\item{...}{Arguments passed to methods.}

\item{network}{Name of the network; default uses \code{DefaultNetwork(object)}.}

\item{celltypes}{Character vector of states/cell types; \code{NULL} for all.}

\item{weight_cutoff}{Numeric threshold for edge weight filtering.}

\item{celltypes_order}{The order of cell types.}

\item{ntop}{The number of top regulators to show (labels and edge filtering).
When \code{filter_by_ntop=TRUE}, only edges from top \code{ntop} regulators are plotted to avoid huge networks.
Default is `10`.}

\item{filter_by_ntop}{Logical. If \code{TRUE} (default), filter edges to top \code{ntop} regulators per celltype.}

\item{title}{The title of figure.
Default is `NULL`.}

\item{theme_type}{The theme of figure.
Could be `"theme_void"`, `"theme_blank"`, or `"theme_facet"`.
Default is `"theme_void"`.}

\item{plot_type}{The type of figure.
Could be `"ggplot"`, `"animate"`, or `"ggplotly"`.
Default is `"ggplot"`.}

\item{layout}{The layout of figure.
Could be `"fruchtermanreingold"` or `"kamadakawai"`.
Default is `"fruchtermanreingold"`.}

\item{nrow}{The number of rows of figure (for \code{facet_wrap} when \code{combine=TRUE}).
Default is `2`.}

\item{ncol}{Integer or \code{NULL}. Number of columns for \code{facet_wrap}.}

\item{byrow}{Logical. If \code{TRUE}, fill facets by row (\code{as.table=TRUE} in \code{facet_wrap}).}

\item{combine}{Logical. If \code{TRUE} (default), return a single faceted plot; if \code{FALSE}, return a named list of plots per celltype.}

\item{figure_save}{Whether to save the figure file.
Default is `FALSE`.}

\item{figure_name}{The name of figure file.
Default is `NULL`.}

\item{figure_width}{The width of figure.
Default is `6`.}

\item{figure_height}{The height of figure.
Default is `6`.}

\item{seed}{The seed random use to plot network.
Default is `1`.}
}
\value{
A dynamic figure object.
}
\description{
Plot dynamic networks
}
\examples{
data(example_matrix, package = "inferCSN")
network <- inferCSN::inferCSN(example_matrix, verbose = FALSE)
network$celltype <- rep(
  c("cluster5", "cluster3", "cluster2", "cluster1", "cluster6"),
  length.out = nrow(network)
)

celltypes_order <- c(
  "cluster5", "cluster3",
  "cluster2", "cluster1",
  "cluster6"
)

plot_dynamic_networks(
  network,
  celltypes_order = celltypes_order
)

plot_dynamic_networks(
  network,
  celltypes_order = celltypes_order[1:3]
)

plot_dynamic_networks(
  network,
  celltypes_order = celltypes_order,
  plot_type = "ggplotly"
)
}
